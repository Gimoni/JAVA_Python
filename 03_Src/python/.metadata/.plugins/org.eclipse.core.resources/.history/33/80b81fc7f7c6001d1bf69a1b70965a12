from util.VT100 import clearScreen
from datetime import datetime
from util.Font import space, dash, column2, number, printFont
from time import time
from EpochTimeTestReview import getDateTime


clearScreen()

year=getDateTime.year()
month = getDateTime.month()
day= getDateTime.day()
date = f'{year:04d}-{month:02d}-{day:02d}'
for i in range(len(date)):
    ch = date[i]
    printFont(ch, 10, 10+i*7)
print()
print(date)

        



year = getDateTime.printDate()
month = 
day = 
hour=
minute=
second=

# epoch = now.strftime("%y-%m-%d %H:%M:%S")

# date = f'{y:04d}-{m:02d}-{d:02d}'
# print(current_milli_time())
# print(epoch)

