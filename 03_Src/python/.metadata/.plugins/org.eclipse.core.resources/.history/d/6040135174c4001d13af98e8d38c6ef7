from util.VT100 import clearScreen, cursorMove, reset
import random as r 

clearScreen()

rect = []

for i in range(20):
    rect.append([False]*40)
    
loopCount = 0
count = 0

while True:
    cursorMove(1, 50)
    reset()
    
    line = r.randint(1, 20)
    column = r.randint(1, 40)
    fg = r.randint(30, 37)
    bg = r.randint(40, 47)
    ch = chr(r.randint(ord('A'), ord('Z')))
    if rect[cursorMove(line-1, column-1)]==False:
        
    pass





print("Program End")






