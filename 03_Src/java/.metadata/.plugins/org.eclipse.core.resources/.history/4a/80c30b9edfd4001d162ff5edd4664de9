
select hiredate, round(hiredate, 'mm') 
  from emp; 
  -- round : single row change ;; 
  
select count(*), min(hiredate), max(hiredate), sum(sal),
	   avg(sal), stddev(sal), variance(sal)
  from emp;