
select hiredate, round(hiredate, 'mm') 
  from emp; 
  -- round : single row change ;; 
  
select count(*),
	   min(hiredate), 
	   max(hiredate), 
	   sum(sal),
	   round(avg(sal), 2),
	   round(stddev(sal), 2),
	   round(variance(sal), 2)
  from emp;